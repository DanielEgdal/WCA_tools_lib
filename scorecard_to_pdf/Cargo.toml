[package]
name = "scorecard_to_pdf"
version = "0.3.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
printpdf = "0.5.2"
font-kit = "0.11.0"
zip = {version = "0.6.2", default-features = false, features = ["time"] }
